<section class="movies" id="movies">
  <h2>{{title}}</h2>
  <div class="row carousel">
    {{#each shorts as |movie|}}
      {{movie-card movie=movie}}
    {{/each}}
    {{#each shorts as |movie|}}
      {{movie-card movie=movie}}
    {{/each}}
  </div>
</section>