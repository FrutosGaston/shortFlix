<section class="movies" id="movies">
  <h2>{{title}}</h2>
  <div class="row carousel">
    {{#each shorts as |short index|}}
      {{movie-card short=short id=index}}
    {{/each}}
  </div>
</section>